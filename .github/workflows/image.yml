name: Build Images

on:
  push:
    branches: ['main','master','dev']
  pull_request:
      paths:
        - docker-compose.yml
        - services
env:
  # Use docker.io for Docker Hub if empty
  # REGISTRY: registry.us-west-1.aliyuncs.com
  REGISTRY: registry.cn-hangzhou.aliyuncs.com
  # github.repository as <account>/<repo>
  # IMAGE_NAME: roweb/automatic1111-sd-webui

jobs:
  build:
    strategy:
      matrix:
        profile:
          - auto
          - invoke
          - comfy
          - download
    runs-on: ubuntu-latest
    name: ${{ matrix.profile }}
    steps:
            # Login against a Docker registry except on PR
      # https://github.com/docker/login-action
      - name: Log into registry ${{ env.REGISTRY }}
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v2
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Log into registry ${{ var.QUAY_REGISTRY }}
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v2
        with:
          registry: ${{ var.QUAY_REGISTRY }}
          username: ${{ secrets.QUAY_USER }}
          password: ${{ secrets.QUAY_TOKEN }}

      - uses: actions/checkout@v3
      - run: docker compose --profile ${{ matrix.profile }} build --progress plain

      # docker images      
      - name: docker images
        if: github.event_name != 'pull_request'
        run: docker images

      # docker tag and push   
      - name: docker tag and push sd-${{ matrix.profile }} to ${{ env.REGISTRY }}
        if: matrix.profile == 'auto' || matrix.profile == 'invoke' || matrix.profile == 'comfy'
        run: | 
          docker images | grep "sd-${{ matrix.profile }}" | awk '{print $1":"$2}' | grep main | xargs -I@ bash -c "docker tag @ ${{ env.REGISTRY }}/roweb/sd-${{ matrix.profile }}:latest"
          docker push ${{ env.REGISTRY }}/roweb/sd-${{ matrix.profile }}:latest

          # docker tag and push   
      - name: docker tag and push sd-${{ matrix.profile }} to ${{ env.QUAY_REGISTRY }}
        if: matrix.profile == 'auto' || matrix.profile == 'invoke' || matrix.profile == 'comfy'
        run: | 
          docker images | grep "sd-${{ matrix.profile }}" | awk '{print $1":"$2}' | grep main | xargs -I@ bash -c "docker tag @ ${{ env.REGISTRY }}/roweb/sd-${{ matrix.profile }}:latest"
          docker push ${{ env.QUAY_REGISTRY }}/luweb/sd-${{ matrix.profile }}:latest